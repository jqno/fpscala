package nl.jqno.fpscala.ch8_testing

import org.scalatest.{FlatSpec, Matchers}

class ParsersTest extends FlatSpec with Matchers {
  // Didn't realise before: we won't have anything executable until near the end of the chapter.
  // Hence: no tests today.
}

